# 性能验证清单

**日期**: 2026-08-12  
**目标**: 确认所有优化已成功启用并工作正常

---

## ✅ 预编译检查

- [ ] 已删除 `build` 文件夹
- [ ] 已运行 `rebuild.bat` 或 `rebuild.sh`
- [ ] CMake 配置成功（无错误提示）
- [ ] 编译成功（最后显示 "✓ 编译成功！"）

---

## ✅ 文件完整性检查

### 验证新文件已创建

```powershell
# Windows
dir MyCompiler\src\optimizer\*.cpp

# 应该看到:
# LoopInvariantCodeMotion.cpp
# StrengthReduction.cpp
# PeepholeOptimization.cpp
# (以及原有的 Optimizer.cpp 等)

# Linux
ls MyCompiler/src/optimizer/*.cpp

# 应该看到上述文件
```

### 验证代码生成优化器

```powershell
# Windows
dir MyCompiler\src\codegen\*.cpp

# 应该包括:
# CodeGen.cpp           (原有)
# CodeGenOptimized.cpp  (新增)
```

---

## ✅ 功能验证

### 测试 1: 所有单元测试通过

```powershell
cd MyCompiler\build
ctest --output-on-failure
```

**预期结果**:
```
100% tests passed, 0 tests failed out of XXX tests
```

**检查项**:
- [ ] test_lexer.cpp: PASSED
- [ ] test_parser.cpp: PASSED
- [ ] test_ast.cpp: PASSED
- [ ] test_semantic.cpp: PASSED
- [ ] test_ir.cpp: PASSED
- [ ] test_optimizer.cpp: PASSED
- [ ] test_codegen.cpp: PASSED
- [ ] test_main.cpp: PASSED

### 测试 2: 单个示例编译

```powershell
# 测试基础示例
.\build\Release\mycompiler.exe ..\examples\arith.src

# 测试复杂示例
.\build\Release\mycompiler.exe ..\examples\test20_complex_expr.src -opt

# 测试循环程序
.\build\Release\mycompiler.exe ..\examples\test07_while.src -opt
```

**预期结果**: 都能正确编译，输出有效的 RISC-V 汇编代码

---

## ✅ 优化启用验证

### 测试 3: 验证每个优化 Pass 已启用

#### 3.1 验证 LICM (循环不变式提取)

**测试代码**:
```c
int f() {
    int i = 0;
    int a = 10;
    while (i < 100) {
        int x = a + 5;      // 循环不变式
        i = i + 1;
    }
    return x;
}
```

**检查 IR**:
```powershell
set DEBUG_IR=1
.\build\Release\mycompiler.exe licm_test.src -opt

# 应该看到: a + 5 的计算在循环外
# ASSIGN x, (a + 5)   # ← 在循环前
# LABEL L0:
# ...循环体...
```

#### 3.2 验证强度削弱 (Strength Reduction)

**测试代码**:
```c
int f() {
    int x = 0;
    x = 10 * 4;        // 应该优化为 10 << 2
    x = 20 / 8;        // 应该优化为 20 >> 3
    return x;
}
```

**检查 IR**:
```powershell
set DEBUG_OPTIMIZER=1
.\build\Release\mycompiler.exe strength_test.src -opt

# 应该在输出中看到:
# [StrengthRed] 乘以 4 → 左移 2 位
# [StrengthRed] 除以 8 → 右移 3 位
```

#### 3.3 验证窥孔优化 (Peephole)

**测试代码**:
```c
int f() {
    int x = 0;
    x = x + 0;         // 应该消除
    int y = x * 1;     // 应该消除乘法
    int z = y * 0;     // 应该变为 0
    return z;
}
```

**检查 IR**:
```powershell
set DEBUG_OPTIMIZER=1
.\build\Release\mycompiler.exe peephole_test.src -opt

# 应该在输出中看到:
# [Peephole] Pass 1: 优化 3 处
```

### 测试 4: 验证命令行参数

```powershell
# 不使用优化
.\build\Release\mycompiler.exe examples\test20_complex_expr.src > out_no_opt.s
# 查看文件大小
ls -l out_no_opt.s

# 使用优化
.\build\Release\mycompiler.exe examples\test20_complex_expr.src -opt > out_opt.s
# 查看文件大小
ls -l out_opt.s

# 对比
# out_opt.s 应该明显小于 out_no_opt.s
# 预期: 大小减少 20-50%
```

**验证**:
- [ ] 无 `-opt`: 生成较大的汇编文件
- [ ] 有 `-opt`: 生成较小的汇编文件
- [ ] 大小比例: -20% 到 -50%

---

## 📊 性能测试

### 测试 5: 运行性能对比测试

```powershell
python test_performance.py --compare
```

**预期输出**:
```
========================================
性能对比测试（优化前后）
========================================

test20_complex_expr.src
------------------------------------------
  汇编行数: 320 → 245 (减少 23.4%)
  编译时间: 0.045s → 0.048s

test07_while.src
------------------------------------------
  汇编行数: 180 → 128 (减少 28.9%)
  编译时间: 0.032s → 0.035s
```

**检查项**:
- [ ] 所有测试都有代码减少
- [ ] 代码大小减少 15-50%
- [ ] 编译时间增加不超过 10%

### 测试 6: 详细性能指标

```powershell
# 运行所有示例
python test_performance.py

# 应该看到每个示例的编译结果
# ✓ arith.src                         245 行汇编, 0.032s
# ✓ test07_while.src                 128 行汇编, 0.028s
# ✓ test20_complex_expr.src           245 行汇编, 0.045s
# ...
```

---

## 🔍 调试验证

### 测试 7: 环境变量调试

```powershell
# 启用优化器调试
set DEBUG_OPTIMIZER=1

# 编译程序
.\build\Release\mycompiler.exe MyCompiler\examples\test07_while.src -opt 2>&1 | head -20

# 应该看到类似:
# [LICM] 完成，找到 1 个循环
# [StrengthRed] 乘以 4 → 左移 2 位
# [Peephole] Pass 1: 优化 2 处
# [Optimizer] 3 轮, 最终 128 条IR
```

**检查项**:
- [ ] LICM 信息出现
- [ ] StrengthRed 信息出现
- [ ] Peephole 信息出现
- [ ] 最终 IR 行数比初始少

### 测试 8: IR 对比

```powershell
# 关闭优化，捕获 IR
$env:DEBUG_IR=1
.\build\Release\mycompiler.exe examples\arith.src 2> ir_no_opt.txt

# 启用优化，捕获 IR
.\build\Release\mycompiler.exe examples\arith.src -opt 2> ir_opt.txt

# 对比文件大小
ls -l ir_no_opt.txt ir_opt.txt

# ir_opt.txt 应该明显小于 ir_no_opt.txt
```

**检查项**:
- [ ] ir_opt.txt 行数少于 ir_no_opt.txt
- [ ] 减少比例 20-50%

---

## 📋 完整性检查表

### 新功能检查
- [ ] LICM 优化已启用
- [ ] 强度削弱已启用
- [ ] 窥孔优化已启用
- [ ] 代码生成优化可用

### 功能正确性检查
- [ ] 所有单元测试通过
- [ ] 示例程序能正确编译
- [ ] 生成的汇编代码有效
- [ ] 优化前后结果一致

### 性能提升检查
- [ ] 代码大小减少 20%+
- [ ] 编译时间增加 <10%
- [ ] 性能指标改善 20%+

### 文档完整性检查
- [ ] 快速开始.txt 可读
- [ ] 性能优化总结.md 可读
- [ ] 性能优化指南.md 可读
- [ ] OPTIMIZATION_PLAN.md 可读

---

## 🎯 成功标准

### 必须满足（✅ GO/NO-GO）

必须全部通过才能认为优化成功集成：

- ✅ 编译成功，无错误
- ✅ 所有单元测试通过 (100%)
- ✅ 至少一个测试文件代码大小减少 >15%
- ✅ 编译时间增加 <10%
- ✅ `-opt` 参数可用

### 预期目标（🎯 TARGETS）

预期性能指标：

- 🎯 平均代码大小减少: 25-35%
- 🎯 含循环程序性能提升: 20-40%
- 🎯 含乘除运算程序性能提升: 15-30%
- 🎯 优化器通过轮数: 2-5 轮
- 🎯 编译开销增加: 3-5%

---

## 📝 验证报告模板

```
╔════════════════════════════════════════════╗
║   编译器性能优化验证报告                   ║
╚════════════════════════════════════════════╝

日期: ___________
系统: Windows / Linux / Mac

1. 编译状态
   □ 成功  □ 失败
   编译时间: ___ 秒
   警告数: ___

2. 单元测试
   □ 全部通过 (XXX/XXX)
   □ 部分通过 (XXX/XXX)
   □ 全部失败

3. 性能测试
   平均代码大小减少: ___ %
   最大代码大小减少: ___ %
   最小代码大小减少: ___ %

4. 优化启用验证
   □ LICM: 已启用，检测到 ___ 个循环
   □ 强度削弱: 已启用，优化 ___ 处
   □ 窥孔优化: 已启用，优化 ___ 处

5. 验证结论
   □ ✅ 优化成功集成
   □ ⚠️  需要进一步检查
   □ ❌ 优化集成失败

备注:
_________________________________
_________________________________

签名: ________________  日期: ________
```

---

## 🔧 问题排查

如果验证失败，请按以下顺序排查：

1. **编译失败**
   - 清除 build 文件夹: `rm -r build`
   - 重新运行编译脚本: `rebuild.bat` 或 `rebuild.sh`

2. **单元测试失败**
   - 查看具体失败的测试
   - 运行 `ctest --verbose` 获取详细信息

3. **性能没有提升**
   - 确认使用了 `-opt` 参数
   - 检查源代码是否有可优化的模式
   - 试试其他示例文件

4. **调试信息不出现**
   - 检查是否正确设置了环境变量
   - 确保重定向到了 stderr (2>&1)
   - 试试直接输出: `DEBUG_OPTIMIZER=1 mycompiler test.src 2>&1`

---

**验证完成日期**: ___________  
**验证人员**: _______________  
**验证结果**: ✅ / ⚠️ / ❌
