# 简易编译器 —— 实现记录

## 推荐实现顺序

按依赖关系从底层到上层，每完成一个模块就跑测试验证：

| 顺序 | 模块 | 文件 | 说明 |
|:---:|------|------|------|
| 1 | 词法分析 | `src/lexer/Lexer.cpp` | 字符流 → Token 流，跑通 `test_lexer` |
| 2 | 语法分析 | `src/parser/Parser.cpp` | Token 流 → AST，跑通 `test_parser` |
| 3 | AST 打印 | `src/ast/ASTPrinter.cpp` | 调试用，树形打印 AST |
| 4 | 符号表 | `src/semantic/SymbolTable.cpp` | 作用域栈，支撑语义分析 |
| 5 | 语义分析 | `src/semantic/SemanticAnalyzer.cpp` | 类型检查，跑通 `test_semantic` |
| 6 | IR 生成 | `src/ir/ThreeAddrCode.cpp` + `src/ir/IRGenerator.cpp` | AST → 三地址码 |
| 7 | 优化 | `src/optimizer/Optimizer.cpp` | 常量折叠、CSE |
| 8 | 代码生成 | `src/codegen/CodeGen.cpp` | 解释执行三地址码 |
| 9 | 串联 | `src/main.cpp` | 组装全部阶段，端到端运行 |

## 构建

```powershell
cd MyCompiler
mkdir build
cd build
cmake ..
cmake --build . -j4
```

## 运行

```powershell
.\Debug\mycompiler.exe ..\examples\arith.src
```

## 测试

```powershell
cd MyCompiler\build
ctest --output-on-failure
```

## 参考文档

- `指导1.tex` —— 编译器设计指导手册（含各阶段详细设计思路）

---

## 阶段一：词法分析器（Lexer）✅ 已完成

> 完成日期：2026-06-10

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/lexer/Token.h` | 修改 Token 结构体（`intValue` 直接成员） |
| `src/lexer/Lexer.h` | 无改动（接口定义阶段已完善） |
| `src/lexer/Lexer.cpp` | 全部 11 个函数实现 |

### 实现顺序（自底向上）

| # | 函数 | 职责 |
|:--:|------|------|
| 1 | `advance()` | 读一个字符，推进 `current_`，更新行列号 |
| 2 | `peekChar()` | 看当前字符，不推进指针 |
| 3 | `peekNext()` | 看下一个字符，不推进指针 |
| 4 | `skipComment()` | 跳过 `//` 直到行尾 |
| 5 | `skipWhitespace()` | 跳过空白 + 调用 `skipComment()` |
| 6 | `scanNumber()` | 扫描数字（支持十/八/十六/二进制） |
| 7 | `scanIdentifierOrKeyword()` | 扫描标识符，查 `keywordMap()` 判关键字 |
| 8 | `scanOperator()` | 扫描运算符/分隔符，含双字符 `== != <= >= && ||` |
| 9 | `nextToken()` | 总调度：skipWhite → 分发到 scan* |
| 10 | `peek()` | 保存/恢复状态 → `nextToken()`，用于 Parser 向前看 |
| 11 | `scanAll()` | 循环 `nextToken()` 直到 EOF |

### 设计决策

- **Token 值存储**：放弃 `std::variant`，改用 `int intValue` 直接成员，简化访问
- **多进制支持**：`scanNumber()` 通过前缀区分进制（`0x`→16、`0b`→2、`0`→8、其他→10），循环条件中分阶段允许 `x`/`b` 字母和 `A-F` 十六进制数字
- **未知字符处理**：返回 `UNKNOWN` 类型 Token，携带原始字符便于错误报告
- **行号列号**：`advance()` 遇到 `\n` 时 `line++`、`column=1`；各 `scan*` 在函数入口用 `startLine`/`startCol` 快照起始位置，不受后续 `advance()` 影响

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | 构造函数初始化列表已设完成员，是否还需要写函数体？ | 不需要，成员已在初始化列表中完成赋值 |
| 2 | `peekNext()` 是返回当前字符的下一个，还是下下个？ | 当前字符的下一个（`current_ + 1`），用于区分 `<=` / `<` 等双字符运算符 |
| 3 | `scanNumber()` 中行列号是否被正确记录？ | 是，构造函数 `Token(NUMBER, "", line_, column_)` 在 `advance()` 之前捕获起始位置，后续推进不影响 |
| 4 | 是否需要支持二进制/八进制/十六进制？ | 课程不要求，但可以实现。通过判断 `0x`/`0b`/`0` 前缀，`std::stoi` 传入不同 base 即可 |
| 5 | `std::stoi` 会自动识别进制吗？ | 不会，默认 base=10。需显式传入 base 参数，且 `isdigit()` 循环已过滤非法字符 |
| 7 | `peek()` 保存恢复的是什么？ | 保存的是 Lexer 的读取状态（`current_`/`line_`/`column_`），不是 Token 属性。Token 的行列号由内部 `scan*` 在构造时记录 |
| 8 | `peek()` 的使用场景？ | 用于 Parser 向前看判断：如 `x = 1`（赋值） vs `x + 1`（表达式），peek 看到 `=` 则走赋值分支 |

---

## 阶段二：语法分析器（Parser）✅ 已完成

> 完成日期：2026-06-11

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/parser/Parser.h` | 新增 2 个表达式层级 + 3 个语句解析声明 |
| `src/parser/Parser.cpp` | 全部 24 个函数实现 |
| `src/ast/AST.h` | 新增 `BreakStmt` `ContinueStmt` `ReturnStmt` |

### 实现顺序

| # | 函数 | 职责 | 层级 |
|:--:|------|------|:--:|
| 1 | `advance()` | Token 推进：`previousToken_` ← `currentToken_` ← `lexer_.nextToken()` | 核心 |
| 2 | `check()` | 判断当前 Token 类型 | 核心 |
| 3 | `match()` | 匹配即消耗（成功 advance，失败不动） | 核心 |
| 4 | `consume()` | 必须匹配，否则抛 `ParseError` | 核心 |
| 5 | `synchronize()` | 恐慌模式错误恢复：跳到 `;` `}` 或关键字 | 核心 |
| 6 | `parseProgram()` | 根入口：循环 `parseStatement()` 直到 EOF | 语句 |
| 7 | `parseStatement()` | 分发器：CONST→跳过+VarDecl, INT→VarDecl, IF→If, WHILE→While, BREAK/CONTINUE/RETURN→对应Stmt, LBRACE→Block, SEMICOLON→空语句, IDENTIFIER→Assignment 或 Expr | 语句 |
| 8 | `parseType()` | 匹配 `int` | 语句 |
| 9 | `parseVarDecl()` | `type IDENTIFIER (= expr)? ;` | 语句 |
| 10 | `parseAssignment()` | `IDENTIFIER = expr ;` | 语句 |
| 11 | `parseIfStmt()` | `if (expr) stmt (else stmt)?` | 语句 |
| 12 | `parseWhileStmt()` | `while (expr) stmt` | 语句 |
| 13 | `parseBreakStmt()` | `break ;` | 语句 |
| 14 | `parseContinueStmt()` | `continue ;` | 语句 |
| 15 | `parseReturnStmt()` | `return expr? ;` | 语句 |
| 16 | `parseBlock()` | `{ stmt* }` | 语句 |
| 17 | `parseLogicalOr()` | `\|\|` 运算，调用 `parseLogicalAnd` | 表达式 |
| 18 | `parseLogicalAnd()` | `&&` 运算，调用 `parseEquality` | 表达式 |
| 19 | `parseEquality()` | `==` `!=`，调用 `parseComparison` | 表达式 |
| 20 | `parseComparison()` | `<` `<=` `>` `>=`，调用 `parseAddition` | 表达式 |
| 21 | `parseAddition()` | `+` `-`，调用 `parseMultiply` | 表达式 |
| 22 | `parseMultiply()` | `*` `/` `%`，调用 `parseUnary` | 表达式 |
| 23 | `parseUnary()` | `-` `!` `+`（右结合），调用 `parsePrimary` | 表达式 |
| 24 | `parsePrimary()` | NUMBER / IDENTIFIER / `( expr )` | 表达式 |

### 递归下降文法（LL(1) 风格，消除左递归后）

```
program    → statement* EOF

statement  → varDecl | ifStmt | whileStmt | breakStmt | continueStmt | returnStmt
           | block | assignment | exprStmt | emptyStmt

varDecl    → ('const')? type IDENTIFIER varDecl'
varDecl'   → '=' expression ';' | ';'

breakStmt  → 'break' ';'
continueStmt → 'continue' ';'
returnStmt → 'return' expression? ';'
emptyStmt  → ';'

assignment → IDENTIFIER '=' expression ';'

ifStmt     → 'if' '(' expression ')' statement ifStmt'
ifStmt'    → 'else' statement | ε

whileStmt  → 'while' '(' expression ')' statement

block      → '{' block'
block'     → statement block' | '}'

exprStmt   → expression ';'

expression → logicalOr
logicalOr  → logicalAnd logicalOr'
logicalOr' → '||' logicalAnd logicalOr' | ε

logicalAnd → equality logicalAnd'
logicalAnd'→ '&&' equality logicalAnd' | ε

equality   → comparison equality'
equality'  → ('==' | '!=') comparison equality' | ε

comparison → addition comparison'
comparison'→ ('<' | '<=' | '>' | '>=') addition comparison' | ε

addition   → multiply addition'
addition'  → ('+' | '-') multiply addition' | ε

multiply   → unary multiply'
multiply'  → ('*' | '/' | '%') unary multiply' | ε

unary      → ('-' | '!' | '+') unary | primary

primary    → NUMBER | IDENTIFIER | '(' expression ')'
```

### 表达式优先级（从低到高）

| 优先级 | 运算符 | 对应函数 | 结合性 |
|:---:|--------|----------|:---:|
| 1 (最低) | `\|\|` | `parseLogicalOr` | 左 |
| 2 | `&&` | `parseLogicalAnd` | 左 |
| 3 | `==` `!=` | `parseEquality` | 左 |
| 4 | `<` `<=` `>` `>=` | `parseComparison` | 左 |
| 5 | `+` `-` | `parseAddition` | 左 |
| 6 | `*` `/` `%` | `parseMultiply` | 左 |
| 7 | `-` `!` `+`（一元） | `parseUnary` | **右** |
| 8 (最高) | 字面量/括号 | `parsePrimary` | — |

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | `check()` 的判断原理？ | `enum class` 本质是整数，`currentToken_.type == t` 即整数判等 |
| 2 | `consume()` 的 `errMsg` 从哪来？ | 调用方传入的字符串字面量，描述期望什么 Token，如 `"Expected ';' after expression."` |
| 3 | `synchronize()` 的作用？ | 出错后不崩溃，跳到下一条语句继续解析，一次编译报告多个错误 |
| 4 | `parseStatement` 能全用 switch 吗？ | 可以，但 INT 需在 switch 外判断（或用两个 case）；IDENTIFIER 需 `peek()` 区分赋值与表达式 |
| 5 | 为什么 IDENTIFIER 需要 peek？ | 只看当前 Token 无法区分 `x = 1`（赋值）和 `x + 1`（表达式），必须偷看下一个 |
| 6 | `currentToken_` 如何变化？ | 只有 `advance()` 改它，`advance()` 被 `match()`/`consume()` 调用，Token 单向推进不回退 |
| 7 | Parser 要判断除数为 0 吗？ | 不需要，Parser 只管语法结构，除 0 是语义/运行时错误 |
| 8 | `BlockStmt` 和 `Program` 的区别？ | 结构几乎一样（都是 `vector<Stmt>`），区别：Program 是整棵树的根，BlockStmt 是 `{}` 包裹的子作用域 |
| 9 | `break` vs `[[fallthrough]]` 在 switch 中？ | `break` 完全跳出 switch，`[[fallthrough]]` 穿透到下一个 case，C++17 属性 |
| 10 | `AssignExpr` 能直接返回为 `Stmt` 吗？ | 不能，`AssignExpr` 继承 `Expr` 非 `Stmt`，需包一层 `ExprStmt` |
| 11 | `const int` 如何处理？ | `parseStatement` 中遇到 CONST 则 `advance()` 跳过，然后照常 `parseVarDecl()` |
| 12 | 空语句 `;` 如何表示？ | `ExprStmt` 的 `expr` 为 `nullptr` |

---

## 阶段三：AST 构建与打印 ✅ 已完成

> 完成日期：2026-06-18

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/ast/AST.h` | 新增 `BreakStmt` `ContinueStmt` `ReturnStmt` 节点 + visitor + accept |
| `src/ast/ASTPrinter.cpp` | 实现 15 个 `visit()` 方法 + `printAST()` |
| `tests/test_ast.cpp` | 新建，包含构造/继承/访问者/打印/所有权测试 + 可视化展示 |

### AST 节点类层次

```
ASTNode（抽象基类，纯虚 accept）
├── Expr（表达式标记）
│   ├── NumberLiteral      int value
│   ├── IdentifierExpr     string name
│   ├── BinaryExpr         unique_ptr<Expr> left; TokenType op; unique_ptr<Expr> right
│   ├── UnaryExpr          TokenType op; unique_ptr<Expr> operand
│   └── AssignExpr         string name; unique_ptr<Expr> value
├── Stmt（语句标记）
│   ├── ExprStmt           unique_ptr<Expr> expr
│   ├── VarDeclStmt        DataType type; string name; unique_ptr<Expr> init?
│   ├── IfStmt             Expr condition; Stmt thenBranch; Stmt elseBranch?
│   ├── WhileStmt          Expr condition; Stmt body
│   ├── BreakStmt          （无子节点）
│   ├── ContinueStmt       （无子节点）
│   ├── ReturnStmt         unique_ptr<Expr> value?（可为 nullptr）
│   └── BlockStmt          vector<unique_ptr<Stmt>> statements
└── Program（根节点）      vector<unique_ptr<Stmt>> statements
```

### 设计关键

- **`Expr` 和 `Stmt` 空壳标记基类**：只继承不添加成员，用于编译期类型约束（`unique_ptr<Expr>` 不能指向 `Stmt`）
- **`ASTNode` 纯虚 `accept()`**：抽象类不可实例化，必须由具体子类实现
- **访问者模式 `accept(*this)`**：节点调用 `v.visit(*this)`，利用虚函数实现二元分派，避免 if/else 类型判断
- **`unique_ptr` 树形所有权**：父节点独占子节点，销毁时自动递归释放整棵树，杜绝内存泄漏
- **`enum class TokenType`** 不能直接 `<<` 打印，需 `tokenTypeName()` 转字符串

### ASTPrinter 实现顺序

| # | visit 方法 | 子节点 | 说明 |
|:--:|-----------|:--:|------|
| 1 | `NumberLiteral` | 无 | `indent(); cout << "NumberLiteral(" << value << ")"` |
| 2 | `IdentifierExpr` | 无 | 同上，打印 name |
| 4 | `BinaryExpr` | left, right | depth++; left→accept; right→accept; depth-- |
| 5 | `UnaryExpr` | operand | depth++; operand→accept; depth-- |
| 6 | `AssignExpr` | value | depth++; value→accept; depth-- |
| 7 | `ExprStmt` | expr | depth++; expr→accept; depth-- |
| 8 | `VarDeclStmt` | init? | 判断非空才递归 |
| 9 | `IfStmt` | condition, then, else? | 打印 Condition: / Then: / Else: 标签 |
| 10 | `WhileStmt` | condition, body | 打印 Condition: / Body: 标签 |
| 11 | `BreakStmt` | 无 | 打印 "BreakStmt" |
| 12 | `ContinueStmt` | 无 | 打印 "ContinueStmt" |
| 13 | `ReturnStmt` | value? | 打印 "ReturnStmt"，有值则递归 |
| 14 | `BlockStmt` | statements[] | for (auto& s : statements) s→accept |
| 15 | `Program` | statements[] | 同上 |

### AST 生成过程示意（以 `int x = 1 + 2;` 为例）

```
源码: int x = 1 + 2;
         │
         ▼ 词法分析 (Lexer)
[INT] [IDENTIFIER:x] [ASSIGN:=] [NUMBER:1] [PLUS:+] [NUMBER:2] [SEMICOLON:;]
         │
         ▼ 语法分析 (Parser 递归下降)
parseStatement → check(INT) → parseVarDecl()
  ├── parseType()     → match(INT)  → DataType::INT
  ├── consume(ID)     → Token "x"   → "x"
  ├── match(ASSIGN)   → true        → 进入初始值解析
  │   └── parseExpression()
  │       └── parseLogicalOr → ... → parseAddition()
  │           ├── parseMultiply() → parseUnary() → parsePrimary()
  │           │   └── match(NUMBER:1) → NumberLiteral(1)
  │           ├── check(PLUS) → true → advance()
  │           └── parseMultiply() → parseUnary() → parsePrimary()
  │               └── match(NUMBER:2) → NumberLiteral(2)
  │           → BinaryExpr(NumberLiteral(1), PLUS, NumberLiteral(2))
  └── consume(SEMICOLON)
  → VarDeclStmt(INT, "x", BinaryExpr(1, +, 2))
         │
         ▼ AST 树结构
Program
  VarDecl(int x)
    BinaryExpr(PLUS)
      NumberLiteral(1)
      NumberLiteral(2)
```

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | `struct Expr : ASTNode {}` 为什么是空壳？ | 标记基类，用于编译期区分表达式和语句，`unique_ptr<Expr>` 不能指向 `Stmt` |
| 2 | `ASTNode` 的两个 `virtual` 作用？ | `~ASTNode()=default`（虚析构保安全），`accept()=0`（纯虚函数强制子类实现） |
| 3 | 不能直接 new ASTNode？ | 对，`= 0` 使其成为抽象类，必须通过具体子类实例化 |
| 4 | `unique_ptr<Expr>` 的含义？ | 独占所有权 + 多态——自动管理生命周期，且只能指向表达式子类 |
| 5 | `VarDeclStmt.init` 存什么？ | 存 `=` 右边的整棵表达式子树（`unique_ptr<Expr>` 支持任意深度的表达式） |
| 6 | `n.left->accept(*this)` 中 accept 的作用？ | 利用虚函数找到节点的实际类型，回调 visitor 对应 visit 方法（二元分派） |
| 7 | `*this` 为什么是 ASTPrinter？ | 当前代码在 `ASTPrinter::visit()` 内执行，`this` 就是 ASTPrinter 对象 |
| 8 | `auto& s : n.statements` 如何工作？ | 范围 for 循环，`auto&` 是引用避免拷贝，逐个遍历 vector 中每个 unique_ptr |

### 测试覆盖（test_ast.cpp）

| 测试类别 | 测试数 | 说明 |
|----------|:-----:|------|
| AST 节点构造 | 13 | 验证每种节点的属性设置 |
| 继承体系与 dynamic_cast | 5 | 验证 Expr/Stmt 标记基类正确工作 |
| 访问者模式（计数访问者） | 10 | 验证 accept 正确分发到对应的 visit |
| AST 打印输出（StringVisitor） | 10 | 验证打印格式正确 |
| 深嵌套结构 | 3 | 验证复杂树形的完整性 |
| unique_ptr 所有权 | 3 | 验证 move 语义和自动析构 |
| dataTypeName 工具函数 | 4 | 验证类型名转换 |
| 边界情况 | 4 | 空 init、null else、大块测试 |
| 可视化展示 | 6 | 手工构建 AST 并打印到终端 |
| **合计** | **58** | |

---

## 阶段四：语义分析器（Semantic）✅ 已完成

> 完成日期：2026-06-20

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/semantic/SymbolTable.h` | 无改动（接口阶段已定义） |
| `src/semantic/SymbolTable.cpp` | 实现 6 个函数 |
| `src/semantic/SemanticAnalyzer.h` | 无改动（接口阶段已定义） |
| `src/semantic/SemanticAnalyzer.cpp` | 实现 15 个 visit + 4 个辅助函数 + 3 处预留 TODO（函数调用类型检查、return 路径检查、main 函数检查）；中英文双语错误消息 |
| `src/utils/ErrorHandler.h` | 新增 `#define LANGUAGE` 和 `MSG()` 宏 |
| `src/parser/Parser.cpp` | 17 处错误消息改用 `MSG()` |

### 符号表（SymbolTable）实现顺序

| # | 函数 | 职责 |
|:--:|------|------|
| 1 | `SymbolTable()` | `push_back({})` 创建全局作用域 |
| 2 | `enterScope()` | `push_back({})` 推入新层 |
| 3 | `exitScope()` | `size()>1` 时 `pop_back()`，保护全局层 |
| 4 | `define()` | 在当前层 `find(name)`，重复返回 false，否则插入 |
| 5 | `lookup()` | `rbegin()→rend()` 从内向外查，返回 `Symbol*` 或 `nullptr` |
| 6 | `lookupCurrent()` | 仅查 `back()` 层 |

### 符号表数据结构

```
scopes_ = vector<unordered_map<string, Symbol>>
  [0] = 全局作用域
  [n] = 第 n 层嵌套作用域
  back() = 当前作用域（栈顶）

Symbol { name, type(INT), scopeLevel, initialized }
```

### 语义分析器（SemanticAnalyzer）实现顺序

| # | visit 方法 | 核心逻辑 |
|:--:|-----------|------|
| 1 | `NumberLiteral` | `currentType_ = INT` |
| 2 | `IdentifierExpr` | `lookup(name)` → 未定义/未初始化报错，否则取 type |
| 3 | `BinaryExpr` | 左 `accept`→取 leftType，右 `accept`→取 rightType，调 `checkBinaryOp` |
| 4 | `UnaryExpr` | `-` 要求 INT 返回 INT，`!` 要求 INT(非0) 返回 INT |
| 6 | `AssignExpr` | `lookup` 检查已定义，`accept` 右值，比较类型，设 `initialized=true` |
| 7 | `ExprStmt` | `expr->accept(*this)` |
| 8 | `VarDeclStmt` | `define` 检查重复，有 init 则 `accept` 并比较类型，设 `initialized` |
| 9 | `IfStmt` | `condition->accept` 检查为非零，递归 then/else |
| 10 | `WhileStmt` | `condition->accept` 检查为非零，递归 body |
| 11 | `BlockStmt` | `enterScope()` → 遍历 statements → `exitScope()` |
| 15 | `ReturnStmt` | 如果有值则 `accept` 推导类型 |
| 16 | `BlockStmt` | `enterScope()` → 遍历 → `exitScope()` |
| 17 | `Program` | 遍历 statements |

> 注：`analyse()` 中保留 3 处 TODO 供实验报告陈述扩展方向。

### 辅助函数

| 函数 | 逻辑 |
|------|------|
| `error(msg)` | `cerr` 输出 + `errorCount_++` |
| `isArithmeticOp(op)` | `+ - * / %` |
| `isComparisonOp(op)` | `== != < <= > >=` |
| `checkBinaryOp(L, op, R)` | 算术→两边 INT 返回 INT；比较→两边同类型返回 INT(0/1)；否则报错 |

### 类型检查规则

| 操作 | 合法 | 结果类型 |
|------|:--:|:--:|
| `int + int` / `int - int` / `int * int` / `int / int` | ✅ | `int` |
| `int < int` / `int == int` 等 | ✅ | `int`(0/1) |
| `-int` | ✅ | `int` |
| `!int` | ✅ | `int`(0/1) |

### 语义分析流程示意（以 `int x = 1; x = x + 1;` 为例）

```
visit(Program)
  ├── visit(VarDeclStmt: int x = 1)
  │   ├── define({x, INT, 0, false})  → true（插入全局作用域）
  │   ├── visit(NumberLiteral(1))      → currentType_ = INT
  │   ├── INT == INT                   → 类型匹配
  │   └── lookup("x")->initialized = true
  │
  └── visit(ExprStmt: x = x + 1)
      └── visit(AssignExpr: x = ...)
          ├── lookup("x")              → Symbol{INT, initialized=true}
          ├── visit(BinaryExpr: x + 1)
          │   ├── visit(IdentifierExpr: x)
          │   │   └── lookup("x")      → INT, currentType_=INT
          │   ├── visit(NumberLiteral(1)) → currentType_=INT
          │   └── checkBinaryOp(INT, PLUS, INT) → INT
          ├── INT == INT               → 类型匹配
          └── lookup("x")->initialized = true
```

### 中英文切换机制

```cpp
// ErrorHandler.h
#define LANGUAGE 1          // 1=中文  0=英文
#define MSG(cn, en) std::string(cn)   // LANGUAGE==1
#define MSG(cn, en) std::string(en)   // LANGUAGE==0

// 使用：
error(MSG("未定义变量: ", "Undefined variable: ") + name);
```

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | `find(name) != end()` 是在挨个寻找吗？ | 不是，`unordered_map::find()` 是 O(1) 哈希查找，`end()` 是哨兵标记"不存在" |
| 2 | 嵌套作用域如何实现屏蔽？ | `lookup()` 用 `rbegin()→rend()` 从内向外查，内层命中即返回，外层自动屏蔽 |
| 3 | `rbegin()` 和 `rend()` 的运行原理？ | 反向迭代器，`rbegin()` 指向最后一个元素，`++` 向第一个方向走，`rend()` 是"第一个之前"的哨兵 |
| 4 | `define()` 的原理？ | 在当前层 `find` 查重，不存在则 `map[key]=val` 插入 |
| 5 | `currentType_` 如何在 accept 后变化？ | 每个 `visit` 把结果写入 `currentType_`（类似返回值寄存器），调用方立即读取 |
| 6 | `BinaryExpr` 的 `currentType_` 传递过程？ | `left->accept` → 读 `currentType_` 为 leftType → `right->accept` → 读为 rightType → `checkBinaryOp` 设最终类型 |
| 7 | `end()` 是空的吗？ | 是哨兵标记，表示"不存在位置"。迭代器范围 [begin, end)，end 指向最后一个元素之后 |
| 8 | 错误计数由谁管理？ | `error()` 统一 `errorCount_++`，不再手动计数，避免重复统计 |

---

## 阶段五：中间代码（IR）— DAG 与三地址码

> 完成日期：2026-07-06

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/ir/ThreeAddrCode.h` | 完整定义 TACOperand / TACInstruction / TACProgram 结构 |
| `src/ir/ThreeAddrCode.cpp` | 实现 `toString()` 和 `print()`（8 种指令类型） |
| `src/ir/DAG.h` | 完整定义 DAGNode / DAG 类 |
| `src/ir/DAG.cpp` | 实现 `makeKey` / `getLeaf`(2) / `getNode` / `print` |
| `tests/test_dag.cpp` | 新建，8 类 12 个测试 |

### 三地址码（ThreeAddrCode）

#### 指令类型

| 类型 | 格式 | 用途 |
|------|------|------|
| `BINARY` | `x = y op z` | 二元运算 |
| `UNARY` | `x = op y` | 一元运算 |
| `ASSIGN` | `x = y` | 赋值/复制 |
| `GOTO` | `goto L` | 无条件跳转 |
| `IF_GOTO` | `if x goto L` | 条件跳转（真时跳） |
| `LABEL` | `L:` | 标签 |
| `CALL` | `call f, n` | 函数调用 |
| `RETURN` | `return x` 或 `return` | 返回 |
| `NOP` | （空） | 空操作 |

#### TACOperand 工厂方法

```cpp
TACOperand::temp("t1")      // 临时变量
TACOperand::var("x")         // 用户变量
TACOperand::constInt(42)     // 整数常量
TACOperand::none()           // 空操作数
```

#### TACProgram 辅助方法

| 方法 | 作用 |
|------|------|
| `newTemp()` | 生成 `t0, t1, t2...` 临时变量名 |
| `newLabel(prefix)` | 生成 `L0, L1, L2...` 标签名 |
| `emit(instr)` | 添加指令到序列 |
| `print()` | 打印全部指令 |

### DAG（有向无环图）

#### 核心机制 —— 缓存去重

```
getLeaf("a")  → 查 cache_["VAR_a"] → 命中返回已有ID，未命中创建新节点
getLeaf(42)   → 查 cache_["INT_42"] → 同上
getNode("+", leftId, rightId) → 查 cache_["+|leftId|rightId"] → 同上
```

- `makeKey(label, left, right)` 生成 `"label|left|right"` 格式的唯一键，用 `|` 分隔避免歧义
- 所有操作数先通过 `getLeaf` / `getNode` 获取 ID，自动消除公共子表达式

#### DAG 节点结构

```
DAGNode {
    id          // 唯一标识（在 nodes_ 中的索引）
    label       // 运算符（"+","*"）或叶节点类型（"VAR","INT"）
    leftChild   // 左子节点 ID（-1 表示叶节点）
    rightChild  // 右子节点 ID
    isLeaf      // 是否是叶节点
    varName     // 叶节点变量名
    intValue    // 叶节点常量值
}
```

#### DAG 安全边界

```
a = 1;  t1 = a + b;  a = 2;  t2 = a + b;
```

直接用变量名缓存会导致 `t1` 和 `t2` 错误共享。安全用法：在基本块内构建 DAG（变量值不变），或使用 SSA 形式。

### 三地址码生成示例

以 `if (x > 0) y = 1; else y = -1;` 为例：

```
0: t0 = x > 0
1: if t0 goto L_ELSE
2: y = 1
3: goto L_END
4: L_ELSE:
5: t1 = -1
6: y = t1
7: L_END:
```

### DAG 构建示例

以 `(b + c) + (b + c)` 为例，节点共享避免重复：

```
AST（5 节点，有冗余）:         DAG（4 节点，共享子表达式）:
      +                            +
     / \                          / \
    +   +                        +   |
   / \ / \                      / \  |
  b  c b  c                    b   c-+
  ___ ___ ___                  ___ ___
  5 个不同节点                  4 个不同节点（b+c 被复用）
```

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | `makeKey` 用 `\|` 还是空格分隔好？ | `\|` 更好，不会与标签内容冲突，保证键唯一性 |
| 2 | DAG 如何区分同名变量的不同版本？ | 见下方「DAG 变量隔离设计」 |
| 3 | 三地址码 `IF_GOTO` 的跳转语义？ | 当 `lhs` 为真时跳转到 label，否则继续下一条；实现时比较值与 0/true |
| 4 | `TACOperand` 为什么不用 `std::variant`？ | 和 Token 一样，`intValue/name` 直接成员更简单直观 |

#### DAG 变量隔离设计

DAG 本身只依赖变量名字符串做去重（`cache_["VAR_x"]`），不追踪变量值的变更。

| 场景 | 安全？ | 说明 |
|------|:--:|------|
| 单个表达式内 | ✅ | 一次 `getNode("+", a, b)` 中 a,b 不会被改 |
| 基本块内（无变量重赋值） | ✅ | `int x=1; int y=x+1; int z=x+2;`，x 不变 |
| 基本块内（有变量重赋值） | ❌ | `x=1; t1=x+b; x=2; t2=x+b;` → t1,t2 错误共享 |
| 跨基本块 | ❌ | 上一个基本块的缓存可能污染下一个 |

**上层模块（IRGenerator / Optimizer）在使用 DAG 时应预留隔离机制：**

1. **按表达式/基本块粒度创建新 DAG 对象**——每个独立的优化窗口新建 `DAG dag;`，不跨窗口复用
2. **变量重赋值时重建 DAG**——遇到 `x = expr` 后，之前含 `x` 的缓存自然失效，新起一个 DAG 实例
3. **`invalidateVar(name)` 扩展（可选）**——给 DAG 加一个方法，赋值时清除缓存中所有用到该变量的 key

当前项目中 DAG 属于底层工具，调用方（未来 IRGenerator/Optimizer）只需遵循粒度隔离即可安全使用，无需改动 DAG 本身。

### 测试覆盖（test_dag.cpp）

| 测试类别 | 测试数 | 说明 |
|----------|:-----:|------|
| makeKey 间接测试 | 1 | 相同操作+操作数返回相同 ID |
| 常量叶节点去重 | 1 | 相同/不同常量 |
| 变量叶节点去重 | 1 | 相同/不同变量 |
| 内部节点去重 | 1 | a+b 共享 vs a+b ≠ b+a |
| 复杂表达式 | 1 | (b+c)+(b+c) 只建 4 节点 |
| 多运算符 | 1 | * + - / 链式运算 |
| 边界情况 | 3 | 单节点、负数、空 DAG |
| 打印输出 | 1 | 不崩溃、输出非空 |
| **合计** | **10** | |

### AST → 三地址码转换（IRGenerator）

| 文件 | 改动 |
|------|------|
| `src/ir/IRGenerator.h` | 17 个 visit 声明 + 辅助方法 |
| `src/ir/IRGenerator.cpp` | 部分实现（见下表） |

**当前实现状态（2026-07-11）**

| 状态 | 函数 |
|:--:|------|
| ✅ 已实现 | `generate`、`opToString`(14 运算符)、`NumberLiteral`、`IdentifierExpr`、`BinaryExpr`(含短路求值)、`UnaryExpr`、`AssignExpr`、`ExprStmt`、`VarDeclStmt`、`IfStmt`、`WhileStmt`、`BlockStmt`、`BreakStmt`、`ContinueStmt`、`ReturnStmt`、`Program`、函数定义 |

---

## 阶段六：优化器（Optimizer）✅ 已完成

> 完成日期：2026-07-11

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/optimizer/Optimizer.h` | 声明 3 个 Pass + 辅助方法 |
| `src/optimizer/Optimizer.cpp` | 全实现：迭代优化主循环 + 常量折叠 + CSE + 死代码删除 |
| `src/optimizer/ConstFold.cpp` | 独立编译单元（实现见 Optimizer.cpp） |
| `src/optimizer/CSE.cpp` | 独立编译单元 |
| `src/optimizer/DeadCodeElim.cpp` | 独立编译单元 |
| `tests/test_optimizer.cpp` | 新建，41 个测试 |

### 优化 Pass

| Pass | 说明 |
|------|------|
| `constantFolding` | 对二元/一元运算：操作数均为编译时常量时直接求值，替换为 ASSIGN 常量 |
| `commonSubexpressionElimination` | 用哈希表记录 `(op,lhs,rhs)→resultTemp`，重复表达式替换为直接赋值；变量赋值后清除缓存 |
| `deadCodeElimination` | 逆向标记活跃指令（从 return/if-goto/goto 等出口出发），删除未标记指令 |

### 迭代策略

主循环 `optimize()` 反复执行三个 Pass，直到指令数不再变化（最多 10 轮）——因为一次 Pass 可能暴露新的优化机会。

---

## 阶段七：代码生成（CodeGen）⚠️ 重构中

> 更新日期：2026-07-11

原解释执行方案已替换为 **RISC-V 32 汇编生成**。

| 文件 | 改动 |
|------|------|
| `src/codegen/CodeGen.h` | 重写为 RISC-V 32 汇编生成器接口 |
| `src/codegen/CodeGen.cpp` | 骨架就绪（标签映射、栈分配、li/lw/sw/ecall），具体 emit 待实现 |

### 调用约定

| 用途 | 寄存器/指令 |
|------|------|
| 返回值 | `a0` (x10) |
| 栈指针 | `sp` (x2) |
| 临时寄存器 | `t0-t2` (x5-x7) |
| 加载常量 | `li reg, value` |
| 加载变量 | `lw reg, offset(sp)` |
| 存储变量 | `sw reg, offset(sp)` |
| 系统调用 | `li a7,93; li a0,0; ecall` |

---

## 阶段八：代码生成（CodeGen）✅ 已完成

> 完成日期：2026-07-28

将三地址码（TAC）翻译为 **RISC-V 32 汇编**，输出到 stdout。

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/codegen/CodeGen.h` | 重写为 RISC-V 32 汇编生成器接口 |
| `src/codegen/CodeGen.cpp` | 完整实现：标签映射、栈分配、指令翻译、程序头尾 |

### 实现顺序（自底向上）

| # | 函数 | 职责 |
|:--:|------|------|
| 1 | `emit()` | 输出一行汇编到 stdout |
| 2 | `allocVarOffset()` | 为变量/临时变量分配栈偏移（4 字节对齐，向下增长） |
| 3 | `loadOperand()` | 将操作数加载到寄存器：CONST_INT→`li`，VAR/TEMP→`lw offset(sp)` |
| 4 | `storeOperand()` | 将寄存器值存回栈：VAR/TEMP→`sw reg, offset(sp)` |
| 5 | `emitPrologue()` | 输出汇编头部：`.text` / `.globl _start` / `_start:` / 栈初始化 |
| 6 | `emitEpilogue()` | 输出程序退出：`li a7,93; li a0,0; ecall` |
| 7 | `generate()` | 主函数：标签映射 + 遍历指令 + 分发翻译 |

### 指令翻译映射表

| TAC 指令 | RISC-V 汇编 |
|----------|-------------|
| `ASSIGN x = y` | `load y→t0` + `sw t0, offset(x)` |
| `BINARY x = y + z` | `load y→t0` + `load z→t1` + `add t0,t0,t1` + `sw t0,x` |
| `BINARY x = y - z` | 同上 + `sub` |
| `BINARY x = y * z` | 同上 + `mul` |
| `BINARY x = y / z` | 同上 + `div` |
| `BINARY x = y % z` | 同上 + `rem` |
| `BINARY x = y < z` | 同上 + `slt t0,t0,t1` |
| `BINARY x = y <= z` | `slt t0,t1,t0` + `seqz t0,t0` |
| `BINARY x = y > z` | `slt t0,t1,t0` |
| `BINARY x = y >= z` | `slt t0,t0,t1` + `seqz t0,t0` |
| `BINARY x = y == z` | `sub t0,t0,t1` + `seqz t0,t0` |
| `BINARY x = y != z` | `sub t0,t0,t1` + `snez t0,t0` |
| `BINARY x = y && z` | `and t0,t0,t1` |
| `BINARY x = y \|\| z` | `or t0,t0,t1` |
| `UNARY x = -y` | `load y→t0` + `neg t0,t0` + `sw t0,x` |
| `UNARY x = !y` | `load y→t0` + `seqz t0,t0` + `sw t0,x` |
| `GOTO L` | `j .Ln` |
| `IF_GOTO if x L` | `load x→t0` + `bnez t0, .Ln` |
| `LABEL L` | `.Ln:` |
| `CALL f, n` | `call func_f` + `sw a0, result`（如有返回值） |
| `RETURN x`（函数内） | `load x→a0` + `ret` |
| `RETURN x`（main 后） | `load x→a0` + `li a7,93` + `ecall` |
| `RETURN`（无值） | `li a0,0` + `ret`/`ecall` |

### 栈布局

```
高地址
  ┌──────────┐
  │  栈底    │ ← sp 初始值
  │  var1    │ sp-4
  │  var2    │ sp-8
  │  ...     │
  │  varN    │ sp-4N
  └──────────
低地址
```

- 每个变量/临时变量占 **4 字节**（int 大小）
- 栈向下增长，偏移量为负（`-4, -8, -12, ...`）
- `allocVarOffset()` 首次遇到变量时分配，后续复用同一偏移
- 预分配 **256 字节**栈空间（`addi sp, sp, -256`），足够大多数简单程序

### 标签映射机制

```
第一遍扫描：收集所有 LABEL 指令
  L_then → .L0
  L_else → .L1
  L_end  → .L2
  func_main → func_main（函数标签不加 .L 前缀）

第二遍生成：引用标签时使用映射后的名称
  goto L_then  →  j .L0
  if t0 goto L_else  →  bnez t0, .L1
```

### 调用约定

| 用途 | 寄存器/指令 |
|------|------|
| 返回值 | `a0` (x10) |
| 栈指针 | `sp` (x2) |
| 临时寄存器 | `t0-t2` (x5-x7) |
| 加载常量 | `li reg, value` |
| 加载变量 | `lw reg, offset(sp)` |
| 存储变量 | `sw reg, offset(sp)` |
| 系统调用 | `li a7,93; li a0,0; ecall` |

### 生成示例

以 `int f() { int x = 1 + 2; }` 为例：

```asm
.text
.globl _start
_start:
addi sp, sp, -256
li t0, 1
sw t0, -4(sp)
li t0, 2
sw t0, -8(sp)
lw t0, -4(sp)
lw t1, -8(sp)
add t0, t0, t1
sw t0, -12(sp)
li a7, 93
li a0, 0
ecall
```

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | `<=` 和 `>=` 为什么需要两条指令？ | RISC-V 只有 `slt`（小于则置1），`<=` 需转换为 `!(y < x)` 即 `slt` + `seqz` |
| 2 | `==` 为什么用 `sub` + `seqz`？ | RISC-V 没有直接比较相等的指令，`x == y` 等价于 `(x - y) == 0`，`seqz` 在值为0时置1 |
| 3 | 为什么用 `t0`/`t1` 而不是 `a0`/`a1`？ | `t0-t2` 是临时寄存器，调用者无需保存；`a0` 保留给返回值 |
| 4 | 函数返回和程序退出如何区分？ | 用 `seenMainCall` 标记：CALL main 之前的 RETURN 是函数返回（`ret`），之后的是程序退出（`ecall`） |
| 5 | `allocVarOffset` 为什么用负偏移？ | RISC-V 栈向下增长，`sp` 指向栈顶，局部变量在 `sp` 下方 |
| 6 | 为什么预分配 256 字节而不是精确计算？ | 简化实现，256 字节可容纳 64 个 int 变量，对课程实验足够 |
| 7 | `CALL` 指令如何处理参数？ | 当前简化实现：参数在 CALL 之前已通过 ASSIGN 存入栈，函数内通过 `lw` 读取；未实现寄存器传参约定 |
| 8 | 标签为什么要映射为 `.L0` 格式？ | RISC-V 汇编约定用户标签以 `.` 开头避免与指令名冲突，同时保证标签唯一性 |

---

## 测试覆盖总览

### 新增测试（2026-07-11）

| 文件 | 测试数 | 内容 |
|------|:--:|------|
| `test_symboltable.cpp` | 11 | 定义/查找/重复/嵌套作用域/全局保护/函数符号 |
| `test_ir.cpp` | 21 | 字面量/算术/比较/一元/短路求值/赋值/if-else/while/break/continue/return/函数 |
| `test_optimizer.cpp` | 41 | 常量折叠(20) + CSE(8) + DCE(9) + 集成(4) |
| `test_codegen.cpp` | 25 | 赋值/算术/比较/一元/if/while/函数返回/汇编结构/边界情况 |
| `test_main.cpp` | 14 | 基础编译/控制流/函数/优化/错误处理/复杂程序/汇编结构/端到端 |

### 全部测试统计

| 文件 | 测试数 |
|------|:--:|
| `test_lexer.cpp` | 5 |
| `test_parser.cpp` | 4 |
| `test_ast.cpp` | 58 |
| `test_symboltable.cpp` | 11 |
| `test_semantic.cpp` | 4 |
| `test_dag.cpp` | 10 |
| `test_new_features.cpp` | 26 |
| `test_ir.cpp` | 21 |
| `test_optimizer.cpp` | 41 |
| `test_codegen.cpp` | 25 |
| `test_main.cpp` | 14 |
| **合计** | **219** |

---

## 阶段九：串联（main.cpp）✅ 已完成

> 完成日期：2026-07-28

将前 8 个阶段组装为完整的编译管线，实现端到端编译：stdin 读源码 → stdout 输出 RISC-V 汇编。

### 涉及文件

| 文件 | 改动 |
|------|------|
| `src/main.cpp` | 补全 9 个阶段的调用代码，替换原有 TODO |

### 编译管线流程

```
stdin (源码)
  │
  ▼ 阶段 1：词法分析
Lexer(source) → Token 流
  │
  ▼ 阶段 2：语法分析
Parser(lexer).parse() → AST (Program)
  │
  ▼ 阶段 3：AST 打印（可选，DEBUG_AST=1）
printAST(*ast) → 树形结构输出到 stderr
  │
  ▼ 阶段 4+5：语义分析（内部使用符号表）
SemanticAnalyzer.analyze(*ast) → 类型检查 + 作用域
  │  有错误 → 输出 "Semantic errors" → exit(1)
  │
  ▼ 阶段 6：IR 生成
IRGenerator.generate(*ast) → TACProgram
  │  可选调试：DEBUG_IR=1 时打印三地址码
  │
  ▼ 阶段 7：优化（可选，-opt 参数）
Optimizer.optimize(*irProgram) → 常量折叠 + CSE + 死代码删除
  │
  ▼ 阶段 8：代码生成
CodeGen.generate(*irProgram) → RISC-V 32 汇编 → stdout
  │
  ▼
stdout (汇编)
```

### 命令行参数

| 参数 | 作用 |
|------|------|
| （无） | 基础编译，输出汇编 |
| `-opt` | 开启优化（常量折叠、CSE、死代码删除） |
| 环境变量 `DEBUG_AST=1` | 打印 AST 树形结构到 stderr |
| 环境变量 `DEBUG_IR=1` | 打印三地址码到 stdout |

### 错误处理

| 阶段 | 错误类型 | 处理方式 |
|------|----------|----------|
| 词法/语法 | `ParseError` 异常 | 捕获后输出错误信息，`return 1` |
| 语义分析 | 类型/作用域错误 | `analyze()` 返回错误计数 > 0 时输出 "Semantic errors"，`return 1` |
| 空输入 | 无源码 | 输出 "[Error] Empty input."，`return 1` |

### 端到端测试示例

输入 `int f() { int x = 1 + 2; return x; }`，输出：

```asm
.text
.globl _start
_start:
addi sp, sp, -256
j .L1
.L0:
li t0, 1
sw t0, -4(sp)
li t0, 2
sw t0, -8(sp)
lw t0, -4(sp)
lw t1, -8(sp)
add t0, t0, t1
sw t0, -12(sp)
lw t0, -12(sp)
sw t0, -16(sp)
lw a0, -16(sp)
ret
li a0, 0
ret
.L1:
call func_main
li a0, 0
li a7, 93
ecall
li a7, 93
li a0, 0
ecall
```

### 编译命令

```powershell
g++ -std=c++17 -I src src/main.cpp src/lexer/Lexer.cpp src/parser/Parser.cpp ^
    src/ast/ASTPrinter.cpp src/semantic/SymbolTable.cpp src/semantic/SemanticAnalyzer.cpp ^
    src/ir/ThreeAddrCode.cpp src/ir/IRGenerator.cpp src/ir/DAG.cpp ^
    src/optimizer/Optimizer.cpp src/optimizer/ConstFold.cpp src/optimizer/CSE.cpp src/optimizer/DeadCodeElim.cpp ^
    src/codegen/CodeGen.cpp src/utils/ErrorHandler.cpp -o build/mycompiler.exe
```

### 运行命令

```powershell
# 基础编译
echo "int f() { int x = 1 + 2; return x; }" | .\build\mycompiler.exe

# 开启优化
echo "int f() { int x = 1 + 2; return x; }" | .\build\mycompiler.exe -opt

# 调试模式（打印 AST）
$env:DEBUG_AST=1; echo "int f() { int x = 1; }" | .\build\mycompiler.exe
```

### 过程中的疑问与解答（供实验报告参考）

| # | 疑问 | 解答 |
|:--:|------|------|
| 1 | 为什么语义分析要放在语法分析之后？ | 语义分析依赖 AST 结构，必须先有语法分析的结果 |
| 2 | 优化阶段为什么是可选的？ | 优化不是必须的，`-opt` 参数控制是否启用 |
| 3 | `unique_ptr<Program>` 的所有权如何传递？ | `parser.parse()` 返回 `unique_ptr`，`main` 中持有，各阶段通过引用 `*ast` 访问 |
| 4 | 为什么用 `std::getenv` 而不是命令行参数控制调试？ | 调试输出不影响正常编译流程，用环境变量更灵活 |
| 5 | 语义分析失败后为什么直接退出？ | 类型错误意味着程序不合法，后续阶段无法正确处理 |