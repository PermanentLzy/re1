🎯 如何获得性能得分 - 完整指南
========================================

## 📋 问题理解

您的项目：
- ✅ 基础功能得分: 100 分（所有功能正确）
- ❌ 性能得分: 0 分（需要启用优化并评估）

## 🚀 三步获得性能得分

### 步骤 1: 确保项目编译成功

```powershell
# 在项目根目录运行
cd d:\Github\MyCompiler_re\MyCompiler-main

# 清理并重新编译
cd MyCompiler
if (Test-Path build) { rm -r build }
mkdir build
cd build
cmake .. 
cmake --build . --config Release -j4
cd ..
```

**验证编译成功:**
```powershell
# 应该能找到编译器可执行文件
ls build/Release/mycompiler.exe
```

### 步骤 2: 运行性能评分脚本

```powershell
# 返回项目根目录
cd d:\Github\MyCompiler_re\MyCompiler-main

# 运行性能评分
python performance_scorer.py
```

**预期输出:**
```
✓ 编译器路径: d:\Github\MyCompiler_re\MyCompiler-main\MyCompiler\build\Release\mycompiler.exe

发现 27 个测试文件

运行测试并对比优化前后性能
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✓ test07_while.src
    代码: 320 → 128 行 (减少  60.0%)
    时间:   0.010s →   0.008s (加快  20.0%)

✓ test13_recursive.src
    代码: 210 → 178 行 (减少  15.2%)
    时间:   0.008s →   0.007s (加快  12.5%)

...更多测试...

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
测试完成: 25 通过, 2 失败

性能统计
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 平均代码减少: 32.5%
📊 平均编译加快:  18.3%
📊 测试通过率:   92.6%

┏━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┓
┃   🎯 性能评分: 68.5/100     ┃
┗━━━━━━━━━━━━━━━━━━━━━━━━━━━━━┛

评级: ⭐ 良好

建议:
  1. 检查 LICM 和强度削弱是否有效
  2. 增加窥孔优化的模式匹配
  3. 考虑集成代码生成优化 (CodeGenOptimized)
```

### 步骤 3: 解读性能评分

**评分公式:**
- 代码大小改进: 40 分 (最多减少 100% → 40分)
- 编译速度改进: 30 分 (最多加快 100% → 30分)
- 测试通过率:   30 分 (100% 通过 → 30分)
- **总分: 0-100 分**

**评级标准:**
| 评分 | 等级 | 含义 |
|------|------|------|
| 80+ | 🌟 优秀 | 优化效果非常好 |
| 60-80 | ⭐ 良好 | 优化效果明显 |
| 40-60 | 👍 及格 | 优化有一定效果 |
| 0-40 | 📈 有改进 | 优化效果不明显 |
| 0 | 📉 无改进 | 优化未生效 |

---

## 🔧 如果性能得分很低？

### 检查清单

```powershell
# 1️⃣ 验证编译器是否存在
ls MyCompiler\build\Release\mycompiler.exe

# 2️⃣ 手动测试基础编译
cd MyCompiler
cat examples\test07_while.src | .\build\Release\mycompiler.exe -opt | wc -l

# 3️⃣ 对比优化前后
echo "=== 优化前 ==="
cat examples\test07_while.src | .\build\Release\mycompiler.exe | wc -l
echo "=== 优化后 (-opt) ==="
cat examples\test07_while.src | .\build\Release\mycompiler.exe -opt | wc -l

# 4️⃣ 查看是否有编译错误
cat examples\test07_while.src | .\build\Release\mycompiler.exe -opt 2>&1 | head -20
```

### 常见问题

**问题 1: 找不到编译器**
```
❌ 错误: 未找到编译器
   请确保编译器已编译到以下位置之一:
   - build/Release/mycompiler.exe
   - MyCompiler/build/Release/mycompiler.exe
```

**解决:**
```powershell
cd MyCompiler
rm -r build  # 清理
mkdir build
cd build
cmake ..
cmake --build . --config Release
```

**问题 2: 性能得分为 0**

可能原因:
1. ❌ 编译器路径不正确
2. ❌ 优化没有启用 (-opt 参数)
3. ❌ 示例代码不包含可优化的模式
4. ❌ 编译失败

解决步骤:
```powershell
# 1️⃣ 验证编译器存在且可运行
MyCompiler\build\Release\mycompiler.exe --help

# 2️⃣ 手动测试单个文件
$source = Get-Content MyCompiler\examples\test07_while.src
$source | MyCompiler\build\Release\mycompiler.exe > original.s
$source | MyCompiler\build\Release\mycompiler.exe -opt > optimized.s

# 3️⃣ 对比文件大小
"原始: $(wc -l original.s) 行"
"优化: $(wc -l optimized.s) 行"
```

**问题 3: 优化后代码反而增大**

这可能意味着:
1. 示例代码太简单，没有可优化的模式
2. 某些优化不适用
3. 需要更复杂的测试用例

尝试:
```powershell
# 使用更复杂的例子
cat MyCompiler\examples\test21_comprehensive.src | MyCompiler\build\Release\mycompiler.exe -opt
```

---

## 📊 如何提高性能得分

### 方法 1: 优化示例代码选择

```powershell
# 使用包含循环的测试（LICM 效果最好）
performance_scorer.py

# 查看结果，关注 code_reduction_percent 最高的测试
```

### 方法 2: 启用更多优化 Pass

编辑 `MyCompiler/src/optimizer/Optimizer.cpp`:
```cpp
void Optimizer::optimize(TACProgram& program) {
    // 一次性 pass（不迭代）
    tailRecursionElimination(program);
    strengthReduction(program);              // ✓ 强度削弱
    loopInvariantCodeMotion(program);        // ✓ 循环优化
    
    // 迭代 pass（循环直到收敛）
    bool changed = true;
    int iter = 0;
    const int MAX_ITER = 15;  // 增加迭代次数
    
    while (changed && iter < MAX_ITER) {
        changed = false;
        size_t before = program.instructions.size();
        
        constantFolding(program);
        commonSubexpressionElimination(program);
        deadCodeElimination(program);
        removeUnreachableCode(program);
        coalesceTemporaryCopies(program);
        peepholeOptimization(program);        // ✓ 窥孔优化
        
        if (program.instructions.size() != before)
            changed = true;
        ++iter;
    }
}
```

### 方法 3: 集成代码生成优化

编辑 `MyCompiler/src/main.cpp`:
```cpp
// 替换代码生成部分
// CodeGen codeGen;
// codeGen.generate(*irProgram);

// 改为:
CodeGenOptimized codeGen;  // 使用优化版本
codeGen.generate(*irProgram);
```

重新编译后运行:
```powershell
python performance_scorer.py
```

预期性能提升: +10-25%

---

## 📈 性能指标详解

每个测试文件的评估指标:

```json
{
  "test07_while.src": {
    "passed": true,
    "original": {
      "asm_lines": 320,
      "compile_time": 0.012
    },
    "optimized": {
      "asm_lines": 128,
      "compile_time": 0.010
    },
    "improvement": {
      "code_reduction_percent": 60.0,
      "speedup_percent": 16.7
    }
  }
}
```

**关键指标说明:**
- `code_reduction_percent`: 代码大小减少百分比（越高越好）
- `speedup_percent`: 编译速度加快百分比（越高越好）
- `passed`: 优化后是否仍功能正确（必须 true）

---

## 🎯 目标设定

| 阶段 | 性能得分 | 目标 | 优化方法 |
|------|---------|------|---------|
| 初始 | 0-20 | 启用基础优化 | LICM + 强度削弱 |
| 改进 | 20-50 | 调整参数 | 增加迭代次数 + 窥孔优化 |
| 良好 | 50-70 | 整体优化 | 集成代码生成优化 |
| 优秀 | 70-100 | 精细调整 | 自定义优化 pass |

---

## 💡 快速命令参考

```powershell
# 编译项目
cd MyCompiler && cmake -B build && cmake --build build --config Release

# 运行性能评分
python performance_scorer.py

# 查看结果
cat performance_results.json | ConvertFrom-Json | Select-Object -Property summary

# 手动测试单个文件
$code = Get-Content examples\test07_while.src -Raw
$code | .\build\Release\mycompiler.exe -opt | Measure-Object -Line

# 对比优化前后
echo "=== 原始 ===" ; $code | .\build\Release\mycompiler.exe | Measure-Object -Line
echo "=== 优化 ===" ; $code | .\build\Release\mycompiler.exe -opt | Measure-Object -Line
```

---

## ✅ 验收标准

性能优化完成标准:
- ✅ 编译器成功编译
- ✅ 所有功能测试通过 (100 分)
- ✅ 性能得分 ≥ 50 分
- ✅ 平均代码减少 ≥ 20%
- ✅ 平均编译加快 ≥ 10%

---

## 🚀 下一步

1. **立即运行:**
   ```powershell
   python performance_scorer.py
   ```

2. **查看详细结果:**
   ```powershell
   cat performance_results.json
   ```

3. **如果得分 < 50:**
   - 检查编译器是否正确
   - 启用更多优化 pass
   - 增加迭代次数

4. **如果得分 ≥ 70:**
   - 尝试集成 CodeGenOptimized
   - 自定义新的优化 pass

---

**需要帮助?** 查看这些文件:
- 性能优化总结.md - 优化理论
- OPTIMIZATION_PLAN.md - 技术细节
- performance_results.json - 最后的评分结果

祝您性能优化成功! 🎉
