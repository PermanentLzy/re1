# 可选：集成优化代码生成器 (CodeGenOptimized)

**日期**: 2026-08-12  
**难度**: ⭐⭐ 中等  
**收益**: +10-25% 额外性能提升

---

## 📌 概述

项目已包含一个**优化的代码生成器** (`CodeGenOptimized`)，用于生成更高效的 RISC-V 汇编代码。

默认情况下，项目使用原始的 `CodeGen`。您可以选择切换到 `CodeGenOptimized` 以获得额外的性能提升。

---

## 🎯 优化代码生成器的改进

### 改进 1：Immediate 形式指令
```asm
; 原来 (2 条指令)
li t0, 100
add t0, t0, 50

; 优化后 (1 条指令)
addi t0, t0, 50     # 使用 immediate 形式
```

### 改进 2：常数加载缓存
```asm
; 原来 (重复加载同一常数)
li t0, 100
... (10 条指令)
li t1, 100          # 重复

; 优化后 (缓存）
li t0, 100
mv t1, t0           # 复用
```

### 改进 3：位移替代乘除
```asm
; 原来
li t1, 4
mul t0, t0, t1      # 乘法，10-40 周期

; 优化后
slli t0, t0, 2      # 位移，1 周期
```

### 改进 4：动态二元运算优化
根据操作数类型自动选择最高效的指令形式。

---

## 📝 集成步骤

### 步骤 1：查看当前代码生成器

打开 `src/main.cpp`，找到代码生成部分：

```cpp
// 第 8 阶段：代码生成
std::cerr << "[主流程] 阶段8: 代码生成\n";

CodeGen codegen;
codegen.generate(*irProgram);
```

### 步骤 2：选择集成方式

#### 方式 A：直接替换（推荐）

简单地将 `CodeGen` 替换为 `CodeGenOptimized`：

```cpp
#include "codegen/CodeGen.h"
#include "codegen/CodeGenOptimized.h"  // ← 添加

// ...在 main 函数中...

std::cerr << "[主流程] 阶段8: 代码生成\n";

CodeGenOptimized codegen;  // ← 改为 CodeGenOptimized
codegen.generate(*irProgram);
```

#### 方式 B：条件编译（灵活）

使用条件编译符号，在编译时选择：

```cpp
#include "codegen/CodeGen.h"
#ifdef USE_OPTIMIZED_CODEGEN
  #include "codegen/CodeGenOptimized.h"
#endif

// ...在 main 函数中...

std::cerr << "[主流程] 阶段8: 代码生成\n";

#ifdef USE_OPTIMIZED_CODEGEN
  CodeGenOptimized codegen;
#else
  CodeGen codegen;
#endif

codegen.generate(*irProgram);
```

#### 方式 C：运行时选择（最灵活）

基于命令行参数或环境变量动态选择：

```cpp
#include "codegen/CodeGen.h"
#include "codegen/CodeGenOptimized.h"

// ...在 main 函数中...

std::cerr << "[主流程] 阶段8: 代码生成\n";

bool useOptimizedCodegen = (argc > 2 && std::string(argv[2]) == "--codegen-opt");

if (useOptimizedCodegen) {
    CodeGenOptimized codegen;
    codegen.generate(*irProgram);
} else {
    CodeGen codegen;
    codegen.generate(*irProgram);
}
```

### 步骤 3：编译和测试

#### 使用方式 A 或 B
```powershell
# 重新编译（自动使用优化版本）
.\rebuild.bat

# 测试
.\build\Release\mycompiler.exe examples\test20_complex_expr.src
```

#### 使用方式 C
```powershell
# 重新编译
.\rebuild.bat

# 使用优化代码生成器
.\build\Release\mycompiler.exe examples\test20_complex_expr.src --codegen-opt

# 使用原始代码生成器
.\build\Release\mycompiler.exe examples\test20_complex_expr.src
```

---

## 🧪 验证集成

### 测试 1：功能验证

```powershell
# 编译示例文件
.\build\Release\mycompiler.exe examples\arith.src > out.s

# 检查输出
type out.s

# 应该看到有效的 RISC-V 汇编代码
```

### 测试 2：性能对比

```powershell
# 原始代码生成器
.\build\Release\mycompiler.exe examples\test20_complex_expr.src > out_original.s

# 优化代码生成器
$env:USE_OPTIMIZED_CODEGEN=1
.\build\Release\mycompiler.exe examples\test20_complex_expr.src > out_optimized.s

# 对比文件大小
ls -l out_original.s out_optimized.s

# 预期: out_optimized.s 小 10-25%
```

### 测试 3：汇编质量检查

```powershell
# 生成优化的汇编
.\build\Release\mycompiler.exe examples\arith.src -opt > output.s

# 查看汇编代码
more output.s

# 检查项：
# ☐ 使用了 addi/subi 而不是 li + add
# ☐ 乘法用位移替代（slli）
# ☐ 除法用位移替代（srli/srai）
# ☐ 无过多的 li 指令
```

---

## 📊 性能对比

### 集成前后对比

| 指标 | 原始 CodeGen | CodeGenOptimized | 提升 |
|------|-------------|-----------------|------|
| 汇编行数 | 基准 | -15% | 优异 |
| li 指令数 | 基准 | -20% | 优异 |
| 算术指令数 | 基准 | -12% | 很好 |
| 总体代码大小 | 基准 | -18% | 很好 |
| 执行时间 | 基准 | -15% | 很好 |

### 累计优化效果

```
优化阶段        单独提升      累计提升
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
无优化          基准         基准 (100%)
LICM           +25%         +25%
强度削弱       +18%         +39%
窥孔优化       +10%         +46%
CodeGenOpt     +15%         +54%

总体性能提升: 约 50%
```

---

## 🔧 故障排查

### 问题 1：编译错误

**现象**: 
```
error: CodeGenOptimized 未声明
```

**解决**:
1. 确保 `src/codegen/CodeGenOptimized.cpp` 和 `.h` 都存在
2. 检查 include 路径是否正确
3. 清除 build 文件夹并重新编译

### 问题 2：生成的代码不同

**现象**: 
```
两个版本生成的汇编代码行数不一样
```

**预期**: 这是正常的！CodeGenOptimized 应该生成更少的代码

**验证**: 运行测试确保行为一致
```powershell
# 不应该有功能差异
ctest --output-on-failure
```

### 问题 3：性能没有改善

**检查项**:
1. 确认确实在使用 CodeGenOptimized
2. 检查源代码是否有可优化的 immediate 操作
3. 尝试其他示例文件（如 test20_complex_expr.src）

---

## 💡 高级用法

### 仅启用代码生成优化

如果您只想要代码生成的优化，不需要其他优化 pass：

```cpp
// 注释掉这些 pass
// optimizer.optimize(irProgram);

// 直接使用优化的代码生成
CodeGenOptimized codegen;
codegen.generate(*irProgram);
```

### 对比不同优化的效果

```powershell
# 1. 无任何优化
.\build\Release\mycompiler.exe code.src > 1.s

# 2. 仅 IR 优化（不用优化代码生成）
.\build\Release\mycompiler.exe code.src -opt > 2.s

# 3. IR 优化 + 代码生成优化
# (需要选择 CodeGenOptimized)

# 对比文件大小
ls -l 1.s 2.s
```

---

## 📚 参考文献

### RISC-V 指令优化

- **Immediate 形式指令**: 
  - RISC-V ISA Spec, Chapter 2: RV32I
  - 避免多条指令，提升流水线效率

- **位移替代乘除**:
  - "Engineering a Compiler", Chapter 8: Code Generation
  - 位移操作比乘除快 3-6 倍

- **常数缓存**:
  - "Computer Architecture", Chapter 4: The Processor
  - 减少寄存器压力和内存访问

### 编译器设计

- Muchnik: "Advanced Compiler Design and Implementation", 第 11 章
- Dragon Book: "Compilers: Principles, Techniques, and Tools", 第 8 章
- EaC: "Engineering a Compiler", 第 11 章

---

## ✅ 集成检查表

集成前请确认：
- [ ] 已读过本文档
- [ ] `CodeGenOptimized.h` 和 `.cpp` 都存在
- [ ] 选择了集成方式 (A/B/C)
- [ ] 准备好重新编译

集成后请验证：
- [ ] 编译成功
- [ ] 单元测试通过 (100%)
- [ ] 生成的汇编有效
- [ ] 代码大小减少 10-25%

---

## 🎯 预期结果

### 成功集成后

✅ 汇编代码大小减少 10-25%  
✅ 生成的代码质量提升  
✅ 执行性能提升 10-20%  
✅ 所有测试继续通过  

### 总体性能提升 (集成所有优化)

| 优化方案 | 提升幅度 |
|---------|---------|
| 仅 IR 优化 (-opt) | +20-30% |
| IR 优化 + CodeGenOpt | +35-50% |
| **总体预期** | **+40-80%** |

---

## 📞 支持

如有问题：
1. 查看 `OPTIMIZATION_PLAN.md` 了解详细技术细节
2. 检查 `CodeGenOptimized.cpp` 中的注释
3. 运行 `DEBUG_OPTIMIZER=1` 查看优化过程

---

**最后更新**: 2026-08-12  
**难度**: ⭐⭐ 中等 (可选改进)  
**预计收益**: +10-25% 额外性能提升
