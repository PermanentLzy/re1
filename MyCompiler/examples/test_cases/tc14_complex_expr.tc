// tc14_complex_expr.tc
// 测试: 复杂表达式组合
int main() {
    int a = 2;
    int b = 3;
    int c = 4;
    // (a + b) * c - (b - a) * (c + 1)
    // = (5) * 4 - (1) * (5)
    // = 20 - 5 = 15
    int result = (a + b) * c - (b - a) * (c + 1);
    return result;
}
